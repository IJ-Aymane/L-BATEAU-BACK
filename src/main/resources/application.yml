server:
  port: 8080

spring:
  application:
    name: Lbateau

  data:
    mongodb:
      uri: ${MONGO_URI:mongodb+srv://l-bateau:l-bateau@l-bateau.c50amf6.mongodb.net/Lbateau?retryWrites=true&w=majority}
      auto-index-creation: true

  mvc:
    view:
      prefix: /WEB-INF/
      suffix: .jsp

  # ── Mailtrap SMTP (test) ─────────────────────────────
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: ${MAIL_USERNAME:6d9cbfd41a7e10}
    password: ${MAIL_PASSWORD:d63cb4c164e07a}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# ── JWT ─────────────────────────────────────────────────
jwt:
  secret: ${JWT_SECRET:MySuperSecretKeyForLbateauApp1234567890ABCDEF}
  expiration-ms: ${JWT_EXPIRATION:86400000}

ai:
  service:
    url: http://localhost:5000

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG