server:
  port: 8080

spring:
  application:
    name: Lbateau

  data:
    mongodb:
      uri: ${MONGO_URI:mongodb+srv://l-bateau:l-bateau@l-bateau.c50amf6.mongodb.net/Lbateau?retryWrites=true&w=majority}
      auto-index-creation: true

  mail:
    host: smtp.gmail.com
    port: 2525
    username: ${MAIL_USERNAME:yassinabicha93@gmail.com}
    password: ${MAIL_PASSWORD:xzza qrsf ofpn gzoz}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          starttls:
            enable: false
            required: false

# ── JWT ─────────────────────────────────────────────────
jwt:
  secret: ${JWT_SECRET:MySuperSecretKeyForLbateauApp1234567890ABCDEF}
  expiration-ms: ${JWT_EXPIRATION:86400000}

ai:
  service:
    url: http://localhost:5000

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
app:
  seed:
    enabled: ${APP_SEED_ENABLED:true}
    admin-password: ${APP_SEED_ADMIN_PASSWORD:admin123}
    manager-password: ${APP_SEED_MANAGER_PASSWORD:manager123}
    client-password: ${APP_SEED_CLIENT_PASSWORD:client123}
